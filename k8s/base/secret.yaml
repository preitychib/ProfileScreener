# Placeholder secret — replace values before apply (do not commit real keys).
#   kubectl -n profilescreener create secret generic profilescreener-secrets \
#     --from-literal=OPENAI_API_KEY=sk-... \
#     --from-literal=GROQ_API_KEY=gsk-... \
#     --dry-run=client -o yaml | kubectl apply -f -
apiVersion: v1
kind: Secret
metadata:
  name: profilescreener-secrets
  namespace: profilescreener
type: Opaque
stringData:
  OPENAI_API_KEY: "REPLACE_ME"
  GROQ_API_KEY: "REPLACE_ME"
